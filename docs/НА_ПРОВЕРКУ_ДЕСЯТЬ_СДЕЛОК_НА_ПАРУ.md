# НА ПРОВЕРКУ: до десяти сделок на одной паре — временное послабление

## Шапка

| Поле | Значение |
|---|---|
| Репозиторий | `simafon1-cyber/repp` |
| Ветка | `claude/metatrader5-trading-system-ids42h` |
| Ревизия на проверку | коммит с этим отчётом (см. историю ветки) |
| Предыдущая проверенная | `63d3884` отчёт по падению сборки 85 |
| `LIVE_TRADING` | в репозитории **False**, не трогал |
| Весь набор тестов | **5448 проверок, 0 сбоев**, код 0 |

**Это изменение риска по прямому решению владельца.** Оно НЕ улучшает
результат — числа ниже. Помечено отдельно и снимается одной строкой.

---

## Задание владельца — дословно

> «Давай разрешим открытые на одной паре до 10 сделок. Пометь это, если
> что просто дальше уберем»

Решение принято **после** того, как я назвал числа и возразил (см.
раздел «Что было сказано до решения»).

---

## Что было сказано до решения

Владелец спросил: «Если он видит, что это прибыльная пара, он может
открывать под две, три, четыре, пять сделок на одной валютной паре,
чтобы была больше прибыль?»

Я ответил четырьмя доводами против:

1. **Ожидание входа отрицательно** — −0,0244 R на сделку до издержек.
   Десять сделок умножают не прибыль, а то, что есть.
2. **«Прибыльная пара» — чаще всего везение.** Вклад направления входа
   измерен: ±0,008 R, и знак меняется от периода к периоду. Это шум.
3. **Концентрация уже била по этому счёту.** В отчёте владельца 85 %
   всех потерь дало одно золото, рисковавшее 6,9 % счёта за сделку.
4. **Это запрещает его собственное правило** — «мартингейл, сетку,
   усреднение, увеличение риска — запрещены полностью».

Владелец подтвердил решение с этими числами перед глазами. Дальше я его
выполняю и больше не спорю.

---

## Что изменено

### Отдельная помеченная настройка, а не правка профилей

```python
# ВРЕМЕННОЕ ПОСЛАБЛЕНИЕ — ПОМЕЧЕНО ОТДЕЛЬНО, СНИМАЕТСЯ ОДНОЙ СТРОКОЙ
# Владелец 03.09.2026, дословно: «Давай разрешим открытые на одной паре
# до 10 сделок. Пометь это, если что просто дальше уберем».
#   0  = не вмешиваться, действует лимит профиля риска (было так всегда);
#   10 = решение владельца от 03.09.2026.
MAX_POSITIONS_PER_SYMBOL_OVERRIDE = 10
```

**Почему отдельной строкой, а не правкой профиля.** Профили риска —
многострочный блок, который `config_migrate` намеренно не переносит в уже
установленную программу: править такой блок вслепую опасно. Отдельная
строка снимается возвратом нуля, и ни один профиль при этом не страдает.
Это и есть «пометь это, если что просто дальше уберем», сделанное так,
чтобы «убрать» заняло одно движение.

**Профили не тронуты вовсе:** у «Агрессивного» в профиле по-прежнему
`max_open_positions=5`, у «Консервативного» — `1`. На это есть проверка.

### Применение

Помощник `main._лимит_на_пару(profile)` используется в обеих точках, где
лимит проверяется: обычный вход и хедж. Мусор в настройке (строка,
отрицательное число, `None`) **не расширяет** предел — непонятное
значение толкуется в сторону прежнего лимита, а не большего риска.

### Настройка доедет до установленной программы сама

Она новая, поэтому обычный перенос допишет её в `config.py` владельца при
первом запуске. Проверено на живом файле: дописывается, значение 10.

---

## ГЛАВНОЕ: десяти сделок всё равно НЕ будет, и вот почему

Лимит сделок на пару — не единственное ограничение. Выше него стоит
**потолок совокупного риска**:

| Величина | Значение у «Агрессивного» |
|---|---|
| Риск на одну сделку | 1,2 % счёта |
| Потолок совокупного риска | 6,5 % счёта |
| **Фактически откроется** | **5 сделок** (6,0 % счёта) |
| Шестая потребовала бы | 7,2 % — больше потолка |

**То есть после этой правки число сделок на паре не изменится.**
Останавливает их не лимит сделок, а потолок риска, и владелец его не
поднимал. Сам я его не трогаю: поднять 6,5 % до 12 % — это решение
другого масштаба, и оно не прозвучало.

### Чтобы это не выглядело как «настройка не работает»

Раньше программа говорила просто «Превышен общий риск по открытым
позициям». Человек поднял бы лимит до десяти, увидел пять и решил бы,
что правка не сработала.

Теперь сообщение называет числа:

```
Превышен общий риск по открытым позициям: уже 6.00% + новая 1.20% >
потолок 6.50%. Именно этот потолок, а не лимит сделок на пару,
ограничивает их число
```

---

## Как проверено

`tests/test_positions_per_symbol.py` — **30 проверок**.

| Сценарий | Что проверяется по факту | Итог |
|---|---|---|
| Настройка 10 | предел равен десяти | пройден |
| Настройка 3 | предел равен трём | пройден |
| Настройка 0 | возвращается лимит профиля — пять | пройден |
| Мусор: `"десять"`, `None`, `-3`, `""` | предел НЕ расширяется | пройден |
| Профиль «Агрессивный» | в нём по-прежнему `max_open_positions=5` | пройден |
| Профиль «Консервативный» | по-прежнему `1` | пройден |
| Настройка задана отдельной строкой верхнего уровня | да | пройден |
| Пометка | есть слова «ВРЕМЕННОЕ ПОСЛАБЛЕНИЕ», слова владельца, дата, измеренный минус | пройден |
| Причина отказа по риску | называет потолок, числа и прямо говорит, что дело не в лимите сделок | пройден |
| Причина отказа по лимиту пары | названа отдельно | пройден |
| Арифметика потолка | 1,2 % и 6,5 % дают пять, а не десять | пройден |

---

## Проверка «наоборот» — с числами

Сломано три вещи: послабление перестало применяться, мусор стал
расширять предел до 99, причина отказа вернулась к общим словам.

```
  СБОЙ В ней назван потолок
  СБОЙ И прямо сказано, что дело не в лимите сделок
  СБОЙ И подставляются настоящие числа, а не общие слова
  СБОЙ 'десять' не даёт больше десяти -> 99
  СБОЙ 'десять' оставляет лимит профиля -> 99
Пройдено: 25   Сбоев: 5
```

После возврата: **30 / 0**.

---

## Чего эта работа НЕ проверяет — честно

1. **Не проверено, что десять сделок лучше пяти.** Измерения говорят
   обратное: ожидание входа отрицательно, значит больше сделок —
   больше потерь, а не прибыли.
2. **Не проверено, что программа умеет отличать «прибыльную пару».**
   Измеренный вклад направления входа ±0,008 R и меняет знак между
   периодами — это шум.
3. **Ничего не проверялось у настоящего брокера.** Пакет MetaTrader5
   работает только под Windows, терминала в облаке нет. Зелёные тесты с
   поддельным брокером **не являются** доказательством поведения у
   настоящего.
4. **Не проверено поведение при десяти реально открытых сделках** — при
   нынешнем потолке риска их столько не бывает.

---

## Изменённые файлы

| Файл | Назначение изменения |
|---|---|
| `ai_scalper_standalone/config.py.example` | помеченная настройка `MAX_POSITIONS_PER_SYMBOL_OVERRIDE = 10` |
| `ai_scalper_standalone/main.py` | помощник `_лимит_на_пару`; применение в обеих точках; причина отказа по риску теперь с числами |
| `tests/test_positions_per_symbol.py` | **новый**: 30 проверок |
| `tests/run_all.sh` | новый файл включён в общий прогон |

---

## Как это снять

Одной строкой в `config.py`:

```python
MAX_POSITIONS_PER_SYMBOL_OVERRIDE = 0
```

После этого действует лимит профиля риска, как было всегда. Ни одна
другая настройка не меняется.

---

## Что сознательно не сделано

1. **Потолок совокупного риска не поднимался.** Владелец просил больше
   сделок, а не больше риска на счёт. Поднять 6,5 % до 12 % — отдельное
   решение, и оно не прозвучало.
2. **Риск на сделку не снижался.** Уменьшить 1,2 % до 0,6 % дало бы
   десять сделок в тот же потолок — но это меняет размер позиции, о чём
   тоже не просили.
3. **Правило проекта не переписывалось.** В `CLAUDE.md` по-прежнему
   стоит запрет на увеличение риска. Это послабление — исключение по
   слову владельца, помеченное и обратимое, а не отмена правила.

---

## Что работа НЕ меняет

* Прибыльности не добавляет. Ожидание входа как было **−0,0244 R до
  издержек**, так и осталось.
* Размер лота, стоп, цель, правила входа — не тронуты.
* Потолок совокупного риска, дневной лимит убытка и лимит просадки — не
  тронуты.
* Мартингейл, сетка, усреднение остаются запрещены.
* Фактическое число сделок на паре сегодня не изменится: их ограничивает
  потолок риска.
