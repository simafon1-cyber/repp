# Ревизия данных: что уже видено, а что нет

Документ отвечает на один вопрос, без которого новое исследование
бессмысленно: **на каких данных уже смотрели результат, а на каких нет.**

Это не формальность. «Проверка на невиданном периоде» — пустые слова,
пока не назван виденный период. А если весь имеющийся период уже виден,
то любая «проверка» на нём будет пересказом, а не проверкой, и отличить
одно от другого постфактум невозможно.

---

## 1. ЧТО УЖЕ ВИДЕНО ЦЕЛИКОМ

Архив `data/history_M5.tar.gz`
(sha256 `9e7b7228599c5c5bf670612ef5430c60014b5318d720a6be306d013d686be520`).

Семь инструментов, пятиминутные свечи:

| Инструмент | Начало | Конец | Дней | Баров |
|---|---|---|---|---|
| EURUSD | 2025-12-11 | 2026-08-14 | 246 | 50 000 |
| GBPUSD | 2025-12-16 | 2026-08-19 | 246 | 50 000 |
| USDJPY | 2025-12-16 | 2026-08-19 | 245 | 50 000 |
| USDCHF | 2025-12-16 | 2026-08-19 | 245 | 50 000 |
| USDCAD | 2025-12-16 | 2026-08-19 | 245 | 50 000 |
| AUDUSD | 2025-12-16 | 2026-08-19 | 245 | 50 000 |
| XAUUSD | 2025-11-26 | 2026-08-19 | 265 | 50 000 |

**Этот период участвовал во всех прогонах без исключения:** поиск входов
(`ENTRY_SEARCH.md`), версии выходов (`EXIT_VERSIONS.md`), базовый прогон
(`BASELINE.md`), модель издержек и журнал сделок (И6), проверка гипотезы о
доле издержек на H1 (И7–И8).

Значит **невиданных пятиминутных данных не осталось ни одного дня.**

Отдельно: разделение внутри архива на TRAIN / VALIDATION / OOS, которое
использовалось в прошлых прогонах, **не делает последнюю четверть
невиданной**. Она резалась из того же архива, который целиком прошёл
через поиск входов. Это записано и в паспорте И8 своими словами:

> ВАЖНО: этот OOS уже был виден на M5, поэтому доказательством наличия
> ПРЕИМУЩЕСТВА он служить не может.

---

## 2. ЧТО ЛЕЖИТ НА КОМПЬЮТЕРЕ СВЕРХ АРХИВА

Папка `ai_scalper_standalone/history/raw` (38 МБ, в git не попадает —
`.gitignore`, строка 116). Она богаче архива:

| Инструмент | ТФ | Начало | Конец | Дней | Баров |
|---|---|---|---|---|---|
| `AUDUSD` | M5 | 2025-12-16 | 2026-08-19 | 245 | 50000 |
| `EURUSD` | M1 | 2026-07-24 | 2026-08-19 | 25 | 25000 |
| `EURUSD` | M15 | 2025-08-15 | 2026-08-19 | 368 | 25000 |
| `EURUSD` | M5 | 2025-12-11 | 2026-08-14 | 246 | 50000 |
| `GBPUSD` | M1 | 2026-07-24 | 2026-08-19 | 25 | 25000 |
| `GBPUSD` | M15 | 2025-08-15 | 2026-08-19 | 368 | 25000 |
| `GBPUSD` | M5 | 2025-12-16 | 2026-08-19 | 246 | 50000 |
| `NZDUSD` | M1 | 2026-07-24 | 2026-08-19 | 25 | 25000 |
| `NZDUSD` | M15 | 2025-08-15 | 2026-08-19 | 368 | 25000 |
| `USDCAD` | M1 | 2026-07-24 | 2026-08-19 | 25 | 25000 |
| `USDCAD` | M15 | 2025-08-15 | 2026-08-19 | 368 | 25000 |
| `USDCAD` | M5 | 2025-12-16 | 2026-08-19 | 245 | 50000 |
| `USDCHF` | M1 | 2026-07-24 | 2026-08-19 | 25 | 25000 |
| `USDCHF` | M5 | 2025-12-16 | 2026-08-19 | 245 | 50000 |
| `USDJPY` | M1 | 2026-07-24 | 2026-08-19 | 25 | 25000 |
| `USDJPY` | M5 | 2025-12-16 | 2026-08-19 | 245 | 50000 |
| `XAUUSD` | M1 | 2026-07-23 | 2026-08-19 | 26 | 25000 |
| `XAUUSD` | M15 | 2025-07-22 | 2026-08-19 | 392 | 25000 |
| `XAUUSD` | M5 | 2025-11-26 | 2026-08-19 | 265 | 50000 |

Из этой таблицы следуют три вещи.

**Первая. На M15 история уходит на четыре месяца глубже, чем на M5.**
M15 начинается 2025-08-15, а самая ранняя пятиминутка (кроме золота) —
2025-12-16. Отрезок **2025-08-15 … 2025-12-11 никогда ни в чём не
участвовал.**

**Вторая. NZDUSD не участвовал вообще ни в одном прогоне.** В архиве его
нет; в исследованиях он не встречается. При этом M1 и M15 по нему есть, а
M5 — нет.

**Третья. Терминал отдал заметно меньше, чем просили.** Программа просила
200 000 пятиминуток, получила 50 000; просила 100 000 минуток и
пятнадцатиминуток — получила по 25 000. Это похоже на ограничение
терминала («Максимум баров в окне» в настройках MetaTrader), а не на
границу доступной истории.

---

## 2в. ГЛАВНОЕ ОТКРЫТИЕ: ВЫГРУЗКА НЕ ДОБАВЛЯЕТ ИСТОРИЮ, А СДВИГАЕТ ОКНО

Владелец сделал повторную выгрузку 21 августа. Сравнение с прошлой дало
неожиданный и неприятный результат.

Терминал отдаёт **фиксированное число последних баров** — ровно 50 000 на
M5 и ровно 25 000 на M1 и M15. Числа не изменились ни на единицу. Значит
это жёсткий предел, а не граница доступной истории.

Следствие, которое легко пропустить: **каждая новая выгрузка сдвигает
начало вперёд**.

| Файл | Начало было | Начало стало | Потеряно |
|---|---|---|---|
| `EURUSD_M15` | 2025-08-15 | 2025-08-20 | 4 дня |
| `EURUSD_M5` | 2025-12-11 | 2025-12-18 | 6 дней |
| `EURUSD_M1` | 2026-07-24 | 2026-07-29 | 4 дня |

То есть повторная выгрузка **не углубляет** историю, а обменивает начало
на конец. И потерянное начало **невосстановимо**: окно уже уехало.

Отсюда правило, которого раньше не было: **старые выгрузки нельзя
затирать новыми.** Каждая выгрузка — одноразовый снимок скользящего окна,
и вместе они покрывают больше, чем любая по отдельности.

### Что было сделано немедленно

Прошлая выгрузка лежала **только** в папке `ai_scalper_standalone/history/`,
которая в git не попадает (`.gitignore`, строка 116). Ветки `history-data`
на GitHub не существует — она так и не создалась. То есть данные жили в
одном экземпляре, на временной машине.

Обе выгрузки склеены и сохранены в репозиторий:
`data/history_all.tar.gz`
(sha256 `317bad261c1040e69c1dc12343c2e4d92f02c62606f14bd2196314f4373bd2c1`).

**Правило склейки, названное вслух.** Объединение по времени. При
расхождении берётся та выгрузка, где бар **не был последним**: последний
бар выгрузки может недосчитать тик — терминал отдаёт его закрытым, а один
тик доезжает позже.

Расхождений нашлось ровно **три** на 24 файла, и все три — именно такие:
отличался только счётчик тиков на единицу, цены совпадали до знака.
Например `AUDUSD_M5` за 2026-08-19 01:35 — `82` тика против `83`.

Целостность склейки проверена: время по возрастанию, повторов нет, все
бары по сетке своего таймфрейма, число колонок везде одинаковое.

### Что дала склейка

| Что | Прибавка |
|---|---|
| M1 по шести парам | около 4 100 баров каждая (≈ 4 дня) |
| M15 по пяти парам | около 270 баров каждая (≈ 4 дня) |
| M5 по семи парам | около 800 баров каждая (≈ 2 дня) |
| `NZDUSD_M5` | новый файл целиком, 50 000 баров |
| `AUDUSD_M15`, `USDCHF_M15`, `USDJPY_M15` | новые файлы целиком |

---

## 3. ЧЕГО НЕ ХВАТАЕТ И ОТКУДА ЭТО ВЗЯТЬ

Чтобы вопрос о преимуществе вообще можно было задать, нужен период,
который не участвовал ни в возникновении идеи, ни в подборе чего-либо.

Лучший способ — **выгрузить часовые свечи прямо из терминала**. Причин
две, и обе существенные.

**Часовая история покрывает годы, а не месяцы.** То же число баров на H1
— это в двенадцать раз больший отрезок времени, чем на M5. Из него виден
лишь последний кусок (с ноября–декабря 2025), а всё, что раньше, —
настоящий невиданный период.

**И это снимает оговорку, которая висит с И8.** Сейчас часовые свечи не
выгружаются, а СОБИРАЮТСЯ из пятиминутных склейкой по двенадцать. Сборка
проверена и детерминирована, но она остаётся реконструкцией — так и
записано ограничением в отчёте по И8:

> Часовые данные собраны из пятиминутных, а не выгружены как H1 —
> сборка детерминирована и хеширована, но это всё же реконструкция.

Настоящая часовая выгрузка убирает эту оговорку совсем.

### Что для этого сделано в программе

`history_export.py` теперь выгружает и H1 (раньше — только M1, M5, M15).
Правка на две строки: часовой таймфрейм связка поддерживала и раньше, его
просто не было в списке по умолчанию.

### Что нужно сделать вам, по шагам

1. Открыть MetaTrader 5 и залогиниться на счёт.
2. **Сервис → Настройки → Графики → «Максимум баров в окне»** поставить
   `Неограниченно`. Это то самое ограничение из пункта 2 — без него
   терминал снова отдаст усечённый кусок.
3. Открыть графики нужных инструментов на H1 и **прокрутить их влево до
   упора** — терминал докачивает историю только для того, что открывали.
   Дождаться, пока прокрутка перестанет добавлять свечи.
4. Запустить выгрузку **только H1**.

   > **Ещё одна моя ошибка в этой памятке.** Команда ниже написана для
   > папки с исходниками. У владельца программа **установлена** — она
   > лежит в `C:\Users\<имя>\AppData\Local\AI Scalper Pro`, файлов
   > `.py` там нет вовсе, они внутри `AI_Scalper_Pro.exe`. Команда
   > честно ответила «нет такого файла», и это было правильно.
   >
   > **Для установленной программы командная строка не подходит:**
   > программа собрана без окна консоли, и даже отработав, она ничего не
   > напечатает. Нужна кнопка в окне.

   **Если программа установлена (обычный случай):** кнопка «Выгрузить
   историю» на вкладке «Система». Она выгрузит все четыре таймфрейма —
   дольше, но безопасно: каждая выгрузка идёт в свой каталог снимка. В
   сообщении будет строка `Снимок: ...` — это путь к нему.

   **Если программа запускается из папки с исходниками:**

   ```
   cd ai_scalper_standalone
   python3 history_export.py --timeframe H1
   ```

   > **Здесь была моя ошибка, и её надо назвать.** В первой редакции этой
   > памятки стояла команда `python3 history_export.py` без ключей. Она
   > **не работала**: в файле не было ни точки входа, ни подключения к
   > терминалу, ни вызова выгрузки. На Windows она завершилась бы **без
   > единого слова и без ошибки** — то есть создала бы уверенность, что
   > данные выгружены, когда не выгружено ничего.
   >
   > Дефект нашёл приёмочный аудит. Настоящая точка входа теперь есть, и
   > она обязана: подключиться к терминалу, взять только названный
   > таймфрейм, показать каждый сохранённый файл и число баров, вернуть
   > ненулевой код при любой ошибке и отпустить терминал в любом случае.

   Кнопка в окне тоже годится, но она выгружает **все** таймфреймы —
   это дольше и сейчас не нужно.

   Ключ `desktop_app.py --export-history` теперь тоже требует явного
   `--timeframe` и ходит через ту же сердцевину, что и команда выше.
   Раньше у него была своя копия выгрузки, и она разошлась с основной:
   не ограничивала таймфреймы, не отпускала терминал при обрыве и
   возвращала ноль даже когда не выгрузилось ничего. Две похожие копии
   расходятся всегда — вопрос только в том, когда это заметят.

5. Прислать мне, что получилось, — я скажу, сколько лет удалось достать и
   какая их часть невиданная.

### Затереть прежнюю выгрузку теперь невозможно

Правило «старые выгрузки нельзя затирать» в прошлой редакции было только
СЛОВАМИ в этом документе. Аудит указал на это отдельно: выгрузка писала в
постоянное имя в режиме перезаписи, и кнопка в окне спокойно затёрла бы
невосстановимое ещё раз.

Теперь:

* каждая выгрузка кладётся в **свой каталог с датой и временем**
  (`history/snapshots/2026-08-21T20-30-00Z/`);
* каталог с уже существующим именем **отвергается**, а не дописывается;
* отдельным рубежом стоит запрет писать поверх существующего файла —
  на случай любого другого пути в коде;
* рядом со свечами кладётся `manifest.json` с отпечатком каждого файла,
  чтобы потом можно было доказать: снимок с тех пор не менялся.

Старая папка `history/raw` при этом **не трогается**: то, что в ней уже
лежит, остаётся на месте.

### Отдельная ловушка: `.exe` обновляется только из готовых сборок

Обнаружено на живом примере. Владелец трижды нажимал «Обновить всё», и
версия не менялась. Причина оказалась не в нём: последняя сборка в
разделе Releases была от **18 августа**, ровно та, что у него стояла.
Правки от 20–21 августа лежали в репозитории, но **в `.exe` их никто не
собрал**.

Кнопка честно ничего не делала: новее нечего скачивать.

**Как это лечится.** На GitHub: **Actions → «Сборка AI_Scalper_Pro.exe»
→ Run workflow**, выбрать рабочую ветку. Через 8–10 минут готовый файл
ложится в Releases, и только тогда «Обновить всё» приносит новую версию.

**Что из этого следует для порядка работ.** Любая правка, которой должен
воспользоваться владелец, доходит до него в три шага, а не в один:

1. правка попадает в репозиторий;
2. **собирается сборка**;
3. владелец жмёт «Обновить всё» и перезапускает программу.

Пропуск второго шага выглядит как «обновление не работает».

### Что делать с получившимся снимком

1. **Не менять в нём ничего.** Каталог вместе с `manifest.json` — целиком
   то, что отдал терминал.
2. Прислать его сюда целиком (или архивом).
3. Дальше по порядку: сверить отпечатки и паспорта → определить, какая
   часть периода **действительно** невиданная → до всякого расчёта
   записать и заверить новый паспорт гипотезы с разделением
   train / validation / OOS.

Только после этого можно считать ожидаемую доходность с полной моделью
издержек. Не раньше.

**Ничего не решайте по результатам на этом шаге.** Смотреть прибыльность
до того, как условия проверки записаны и заверены, — ровно та подмена,
против которой написан весь раздел 3 правил проекта. Порядок будет такой:
данные → заверенный паспорт с условиями → прогон → результат.

---

## 4. ЧЕГО Я НЕ МОГУ СДЕЛАТЬ САМ

Выгрузку. Она идёт из вашего терминала на вашем компьютере: здесь нет ни
MetaTrader 5, ни доступа к вашему счёту, а библиотека для связи с
терминалом существует только под Windows.

Это не отговорка, а границы: **чужие котировки не годятся.** У разных
брокеров отличаются и спред, и время сервера, и сами свечи. Проверка на
чужих данных проверяет чужую систему.

---

## 5. ЧЕСТНО О ТОМ, ЧТО ЭТО НЕ РЕШАЕТ

Даже полный невиданный период **не обещает**, что преимущество найдётся.
Ни один замер за всю работу не показал положительного ожидания после
издержек. Невиданные данные дают возможность честно **проверить**
гипотезу — и с тем же успехом честно её **отвергнуть**.

Отрицательный результат на невиданном периоде будет считаться
результатом, а не поводом искать другой период.
