# Нужно ТОЛЬКО для сборки .exe (build_exe.bat), не для обычного запуска бота.
# Версия закреплена НЕ ЖЁСТКО, но с потолком. Раньше стояло >=6.0.0, то есть
# каждая сборка брала самую свежую версию упаковщика — и могла сломаться сама
# по себе, без единой правки в коде. Потолок отсекает смену старшей версии
# (там меняются правила упаковки), нижняя граница оставляет исправления.
pyinstaller>=6.10,<7
